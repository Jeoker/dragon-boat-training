# Cloudflare 数据服务与迁移计划

> 目标架构已确认；2026-09-19 结合现行源码及官方文档复核并补齐实施约束。本文维护计划与验收门槛，不用勾选项代替真实证据；实际实现、部署和未验证边界统一见[当前进度](CURRENT-STATUS.md)与阶段验收记录。
> 本文是 Cloudflare 目标架构、同步协议和 C0–C4 实现路径的唯一维护位置。业务规则引用[项目说明](README.md)，工作归属沿用[三个 Epic](epics/README.md)。

## 交付目标

GitHub Pages 继续承载队员页、Coach Mode 和荣誉墙。Cloudflare Workers + Durable Objects（SQLite）承担在线业务数据与规则；网页日常读取、登录、报名、取消、换侧和排座不等待 Google。Google Form 保留入队登记，Sheets 保留每季运营副本及年度私有归档，支持偶尔直接修改业务单元格后校验导入。

这里的“网页缓存”是共享、持久化的在线主数据，不是浏览器缓存或易失内存。只有 SQLite 已可靠提交才确认网页保存成功；Google 同步状态单独报告。没有人打开网页时，持久任务仍需继续执行。

建季绑定检查、首次名单导入、新成员刚提交 Form、手动表格导入及年度文件生成仍依赖 Google。管理操作先返回持久任务编号，页面查询进度；只有绑定和初始化完成后才激活赛季。新成员导入前提示稍后刷新，Google 中断时不能提前确认入队成功。已导入成员的日常训练业务继续使用 Cloudflare。

## 组件、数据归属与运行边界

| 组件 | 输入与输出 | 触发及职责 | 失败边界 |
|---|---|---|---|
| GitHub Pages | 业务动作、当前版本 → 已确认结果及视图 | 复用现有客户端与组件；进入、恢复前台、手动刷新和写后更新 | 结果未知沿用原请求编号；不在浏览器提前确认名额 |
| Worker | HTTP 请求 → DO 调用与响应 | 固定团队路由、请求格式、来源配置、基本限流；公开及管理字段筛选 | 拒绝任意文件／对象路由；Google 凭据不进入网页 |
| `TeamState` DO + SQLite | 本队业务状态、动作 → 事务结果、版本、事件与待同步任务 | 首版每支队伍一个 DO；同队的成员资格、赛季、报名、排座及管理凭据在同一事务边界内，表键包含 `season_id` | 事务失败不确认；重启从 SQLite 恢复，不依赖内存常驻 |
| 持久任务调度 | 到期任务、检查点 → 下一轮任务及状态 | 同一 DO 的 alarm 调度同步、预约开放、冻结及归档请求；一次只处理有界批次 | 失败持久记录并退避重试；外部网络等待不占住业务事务 |
| Apps Script 桥接 | 服务端签名命令、绑定和批次 → Google 读取、差异与写入回执 | 复用 Form／Sheet 授权、绑定校验、分批导入与年度文件创建 | Google 故障积压同步；不影响已导入成员的正常训练业务 |
| Google Form／Sheets | Form 回答、人工单元格改动 ↔ 私有副本／归档 | Form 增量导入，业务表三方对比，归档按冻结快照导出 | 不从表格任意状态值直接重建名额、正式 revision 或权限 |

首版采用团队级 DO 是为了让跨场次停用成员、默认赛季切换和权限检查保持简单；不按浏览器、请求或座位创建对象。若未来支持其他队伍，必须分配独立对象。读取按赛季／场次及游标分页；索引、历史数据大小和对象存储增长纳入监测，不能每次请求加载全部赛季。扩容前先测量，再决定是否拆分归档存储。

持久数据至少包括现有业务实体、Code 校验材料与会话摘要、请求去重结果、审计事件、`sync_baselines`、`sync_outbox`、`sync_conflicts`、`source_imports`、`scheduled_jobs` 和 `migration_state`。原始 Form 私人回答只导入业务必需字段；敏感字段不进入公开视图或普通诊断日志。

### 事务与异步执行

- 同步 SQLite 事务内完成规则读取、版本核对、业务变化、不可变请求结果、事件和 outbox；使用存储事务 API，不用原始 `BEGIN`／`COMMIT`。事务中不等待 Google，SQL 游标在下一次 `await` 前读完。
- 请求去重键包含团队、调用者作用域、动作及 `request_id`，同时核对参数摘要。相同键不同参数拒绝；已提交请求返回原结果，当前视图单独读取。管理请求的重放也必须先验证当前权限。
- 调度器先用短事务领取任务，持久保存批次、租约及尝试编号，再执行外部 I/O；回来后重新核对任务、业务版本、绑定及 `writer_epoch`。DO 在网络等待期间仍可能处理其他请求，过期回执不能覆盖新状态。
- 同一绑定的导出批次按序执行。超时只表示结果未知；旧批次先查回执并核验，不能因租约到期就让新批次越过它。其他独立记录可继续同步，普通报名不等待该批次。
- 每次产生到期任务都确保安排 alarm；构造恢复和 alarm 收尾均从持久任务表恢复最早时间。已提交但尚未重新设闹钟、任务执行中重启、旧回执迟到均列入故障注入。测试模式禁止对真实 Google 执行副作用。

## 复用和替换范围

- 保留业务 ID、队列时间／序号、对象版本、草稿与正式 revision、冻结姓名、取消标记和审计来源。复用 P1–P5 场景与页面交互，抽出可测试的业务规则；Apps Script 的 Spreadsheet／Utilities／LockService 调用由新的存储、时间和加密适配器替代。
- Cloudflare 报名事务同时完成容量校验、递补、草稿清理、必要的系统 revision、事件、幂等结果与 outbox；SQLite 提交后即可返回。Google 写入不加入这次事务，也不能在导出时重新运行递补算法。
- 管理权限仍只有一套个人 Coach Code。Cloudflare 本地验证凭据和会话，停用／重置立即影响后续管理请求。Code 的人工发放改由受控维护命令执行，不建立在线权限管理产品。
- C0 验证既有摘要算法的迁移：需迁移 salt、摘要及其依赖的服务端 secret，单独复制摘要无效。现行 `CODE_SECRET` 还参与请求键、参数摘要及部分业务 ID 的生成；兼容测试须覆盖 UTF-8、HMAC-SHA256、无填充 base64url 和旧动作构造参数的 `JSON.stringify` 字段顺序，不能直接换成新的排序序列化。既有 ID 原样导入；新算法须显式带版本。secret 仅通过服务端配置转移，旧会话在切换时失效，用户使用原 Code 重新登录。
- Apps Script 不再对已切换团队运行自己的报名／排座／预约发布／冻结算法。Form 导入的事件接收可以保留，但只能送入 Cloudflare 统一状态变化流程；Google 文件生成属于可重试外部任务。
- 训练结束后 24 小时的冻结和赛季结束判断以 Cloudflare 当前状态、服务器时间及事件为准，不等待 Sheet 同步。整季私有归档回执尚未核验时保持归档待完成；公开历史服务只读取已核验的安全投影。

## 同步节奏

以下为首版默认值，可按实测调整；十分钟是正常运行目标，不是故障时的保证。

| 任务 | 节奏 | 限制 |
|---|---|---|
| 网页业务读写 | 即时访问 Cloudflare | 浏览器名单十分钟有效期仍与训练余位刷新分开；名单版本变化提前失效 |
| Cloudflare → Google 运营副本 | 第一笔待同步变更后十分钟内启动一批；持续操作不反复延后这个截止时间 | 无变更不写；批次固定版本，后续变更进入下一批；手动“立即同步”可提前执行 |
| Google Form → Cloudflare | 提交事件增量导入；活跃赛季每十分钟补扫 | 稳定来源键去重；网页打开不触发整表扫描；截止后拒绝新的入队导入 |
| Sheet 人工修改检查 | 活跃赛季每十分钟分批检查已登记业务范围；支持“检查并导入表格修改” | 即使 Cloudflare 没有待导出变化，也必须检查，才能发现直接改表；不扫描全部 Drive／历史 |
| 已结束赛季与归档文件 | 正常业务范围停止周期同步；归档未完成继续重试；管理员可手动核对历史修改 | 冻结内容只能转为更正事项，不直接改变快照；任意历史文件不承诺十分钟发现 |
| 预约发布、冻结和赛季完成 | 按最早到期时间唤醒并有界执行 | 读写实时检查边界；任务延迟不放宽截止 |

单个 DO 同时只有一个 alarm。SQLite 保存各类任务及 `due_at`，每次调度最早任务；先处理到期业务边界，再处理有界同步批次，预算耗尽保存检查点。不能用进程内 `setTimeout` 承担可靠任务，也不能把 outbox 当作已同步记录。

“暂停导出”只暂停该季 Google 运营副本写入，不停止网页报名、Form 入队、时间边界或差异读取。恢复时先处理未确认批次并重新比较表格，不直接发送暂停前积压的旧补丁。已发出的请求无法撤回，界面只有在相关批次完成核验后才显示“已暂停，可以编辑”。

## Google 双向同步协议

### 1. 比较与业务校验

每条受支持记录保存三方比较基线：上次双方确认的值 `B`、Cloudflare 当前值 `C`、本轮读取的 Sheet 值 `G`，连同稳定 ID、字段映射、业务版本及内容摘要。

| 比较结果 | 动作 |
|---|---|
| `C = B` 且 `G = B` | 不写业务数据 |
| 只有 `C` 变化 | 生成向 Google 的有限字段补丁 |
| 只有 `G` 变化 | 转成明确业务命令，按当前规则校验后导入；导入产生新版本、事件及必要递补 |
| `C = G` 且均不同于 `B` | 记录双方已一致，不重复执行业务 |
| 两侧均有变化，字段及业务依赖独立 | 在校验合并后的完整状态后合并 |
| 相同字段不同值，或资格／容量／带队角色等依赖冲突 | 保存双方及基线，建立冲突；暂停该实体相关字段的同步，其余独立记录继续 |

不同字段不必然独立：一侧更换偏好、另一侧取消报名或改变角色，必须作为同一报名／场次的业务冲突处理。比较不能依赖用户可改的 `updated_at`，也不能用“最后写入者获胜”。

正常网页业务在同步冲突期间仍可继续，冲突解决时必须核对新的 Cloudflare 版本和 Sheet 内容摘要；冲突预览过期后重新生成。选择表格值仍须通过容量、截止、资格、归档等检查。

基线按字段及业务依赖组推进。需要确认的 Sheet 改动从发现起就保留原值并阻止相关导出，不能等待用户确认期间被网页值覆盖。报名偏好、确认名额和带队角色按整场相关状态校验；同一实体其他字段仅在明确独立时继续同步。字段映射使用稳定 ID，统一空值、布尔、偏好及带时区时间的比较方式；删除行不能与空字段混为一谈。C2 先交付字段映射及依赖表，再实现合并。

### 2. 哪些表格改动可以导入

| 改动 | 首版处理 |
|---|---|
| 既有成员的姓名覆盖值、默认偏好 | 无冲突且合法时自动导入；Form 源字段不覆盖人工覆盖值 |
| 既有有效报名的左右偏好 | 使用同一换侧动作，保留原队列时间，释放与递补按当前状态计算 |
| 启停成员、取消报名／训练、调整训练日期时间或赛季边界 | 生成影响预览，管理员确认后执行，复用网页相同的高影响操作要求 |
| 角色或座位单元格 | 按整场草稿检查唯一性、角色互斥及容量；合法且无冲突时导入草稿，不自动发布 |
| 删除整行、插入无编号行、重复／修改 ID、未知列结构 | 建立待核查项，保留现场；不猜测取消、不按名字合并、不自动覆盖损坏行 |
| 手改容量结果、候补状态、队列时间、版本、日志、已发布 revision、冻结快照或凭据 | 不作为直接赋值导入；提示使用对应业务／维护操作。冻结记录仅能追加更正说明 |

Form 正常新回答仍自动入队，不增加管理员审批。周期比较覆盖登记的可编辑 Tab；原始回答、系统字段和档案变动由对应导入／完整性检查处理。自动导入事件来源记录为 `SHEET_IMPORT`；不能假定能可靠获知每次 Google 编辑者的身份。

### 3. 导出、回执与重试

1. Cloudflare 在业务事务内同时保存当前变化、操作事件与 outbox。批次包含 `batch_id`、团队／赛季／绑定版本、写入归属代次 `writer_epoch`、实体版本、上次 Sheet 摘要、目标值及事件范围。
2. 桥接先读取相关范围、返回人工差异；协调完成后，只对基线匹配的字段执行补丁。写前持久保存预期变化及已读取的 Sheet 前值，脚本锁只用于串行脚本操作。不得整表覆盖，不改 Form 回答区域。
3. Google 写入允许跨 Tab 分段，桥接保存已完成部分并重读校验；只有对应版本和内容校验通过才返回回执。传输结果未知时用相同批次编号确认，不能重新生成另一个批次盲写。
4. Cloudflare 收到回执只推进批次所涵盖版本的基线和事件游标，不能清除之后的新变更；Sheet 同步后的人工变化属于下一轮。事件逐条保留，即使当前状态补丁被合并也不丢操作顺序。
5. 错误、未确认回执和重试次数持久保存，按退避重新安排 alarm；平台自动重试有次数上限，应用需要继续调度。持续失败在 Coach Mode 显示积压数量、最早待同步时间及处理入口。

Google 表格的人工编辑不受 Apps Script 锁约束，“检查再写”不是数据库级条件更新。写前重读、最小字段补丁、写后核验、变更提示及同步记录可缩小冲突窗口，但不能保证捕获恰在覆盖窗口发生的每一次人工编辑。可安装的编辑触发器只提供提示，不能单独承担完整变更日志或替代周期核对。C2 必须实测并在运维说明如实记录这一边界；批量人工维护可先在管理页暂停该季导出，编辑后检查差异再恢复，未暂停的偶尔改动仍走同一检测路径。

### 4. Form 来源与旧数据衔接

现行 `backend/src/SeasonActions.gs` 使用 `response_sheet_id:row_number` 去重，不能把可移动行号继续当作新桥接的稳定来源 ID。新导入使用 `season_id + form_id + FormResponse.getId()`，Form 提交触发与周期补扫共用该键；相同回答不重复入队，不按姓名合并。补扫使用有重叠的时间窗口及持久游标，只有整批提交成功才推进，覆盖触发器漏送和相同提交时间。

C2 建立旧 `source_key`／`member_id` 与 Form 回答 ID 的映射；保留旧成员编号、覆盖姓名及报名引用。只自动关联证据唯一的记录；重名、历史排序、原回答已删除或无法唯一对应时进入迁移核查，不重新创建队员。C4 前必须解决会影响后续导入的歧义。切换后的 Form 来源字段与成员人工覆盖字段继续分开，原始回答表不作为任意新增成员行的入口。

### 5. Google 桥接传输

采用以文件维护者身份运行的 Apps Script Web App，授权由 Google 文件维护者完成；Cloudflare 的服务请求使用独立共享密钥签名。签名信封放在 POST JSON 正文，通过 `e.postData.contents` 读取，不依赖 Apps Script 未承诺提供的自定义请求头。签名绑定协议版本、方向、团队、绑定版本、归属代次、时间戳、nonce、批次／操作编号及负载摘要。

传输 nonce 与业务 `batch_id` 分开：重试可使用新的有效时间戳和 nonce，但批次编号和负载不变。重复批次查询已保存回执，不能再次执行业务；回执绑定批次、负载及实际完成范围。密钥只存在两端服务配置，不随正文传输。桥接批次状态保存在私有持久存储中，不能仅靠缓存。

Cloudflare 处理 Content Service 的 Google 重定向并校验最终响应结构；登录 HTML、限额页、HTTP 成功但业务失败、超时及响应截断均不能认作回执。C0 在独立脚本验证签名往返及拒绝错误请求；C2 再验证有副作用的分段读写与恢复。

## 接口与页面交付

业务接口保持现有动作、ID、结果／当前视图区分和版本冲突语义。C0 建立单独的迁移契约草案与兼容测试，C4 前不把新端点写成现有生产契约已经支持。

新增受保护能力：同步概览、立即同步、检查表格改动、分页冲突列表、影响预览及冲突解决、暂停／恢复某季导出。冲突解决携带两侧观察版本及 `request_id`；同步按钮返回任务已接受，不假称 Google 已完成。

Coach Mode 仍为四个面板：在“赛季与表单”显示最后同步时间、待同步／失败／冲突及手动入口；在“操作记录”查看导入、合并和解决详情。并列显示基线、网页当前值与 Sheet 值，以自然语言解释影响。普通队员看到 Cloudflare 确认的报名或候补结果，不等待同步，也不接触内部批次字段。

前端迁移使用受控 API 配置和 `backend_generation`／实例标识隔离缓存、令牌及待确认请求。换到新后端后，旧待确认请求先查迁移后的去重记录；不自动把旧动作生成新编号重发。已完成请求的原结果、编号及参数摘要随数据迁移。

实例标识区分环境及数据集，`backend_generation` 标记客户端缓存／会话边界，`writer_epoch` 防止旧服务任务写入；契约明确三者，不能只检查 URL。旧管理操作在重新登录后才能查询，旧登录／退出记录仅作历史证据，不能重放出有效旧会话。首版在线余位及草稿直读 DO，不另加可能过期的边缘缓存；沿用名单版本缓存和已有页面刷新，不要求 WebSocket 或持续实时推送。

## C0–C4 顺序与阶段门槛

继续沿用 BE、FE、ADM 三个 Epic，不建立第四个 Epic。以下顺序先在本地和独立测试环境执行；只有 C4 才切换生产写入归属。

| 阶段 | 主责与交付 | 进入下一阶段前的证据 |
|---|---|---|
| **C0 基础与契约** | BE-09：Worker、团队 DO、SQLite 迁移、事务／请求去重、alarm 最小闭环、代码与部署环境隔离；验证旧摘要兼容及 Google 桥接签名；FE-07 定义 API 代次和错误兼容 | DO 重启后数据、去重结果及任务仍在；并发重复请求只提交一次；事务中途失败不留半份数据；签名与归属错误被拒绝；源码无 secret；保存响应不依赖 Google |
| **C1 业务迁移** | BE-09：按赛季／成员与权限 → 排期 → 报名候补 → 排座 → 冻结历史的顺序迁移；接入 outbox；复用并适配现有测试。FE-07／ADM-07 接测试端点 | 现有业务场景在新存储通过；换侧、取消、最后名额、角色冲突、私有草稿、24 小时、跨季和历史姓名正确；Google 断开时已导入业务仍可使用 |
| **C2 Google 桥接与同步** | BE-10：Form 稳定 ID 导入及旧来源映射、三方比较、有限补丁、可恢复回执、人工编辑检测、完整性提示与年度归档导出 | 独立 Google 文件中验证重复事件、两侧独立／关联变更、排序／删行／改 ID、部分写入、回执丢失或迟到、批次期间继续报名、暂停导出及 Google 停机后恢复；不重复递补或导出日志；归档业务部分可从 DO 快照校验，原始回答部分另有来源清单与摘要 |
| **C3 页面与全流程** | ADM-07：同步概览、暂停导出、预览和冲突解决；FE-07：公开页面、恢复刷新、缓存代次；BE 提供相同测试数据 | 两个真实页面完成报名／换侧／排座，Sheet 改名自动导入、冲突人工解决；预览过期不覆盖；退出清除私有状态；页面不被 Google 同步阻塞 |
| **C4 切换与运行验收** | BE-11 + FE-07：只读影子导入对账、写入交接、前端切换、旧端点防误写、备份恢复及回退演练、交接文档 | 全量迁移清单核对成功；旧标签页和旧任务不能写；隔离环境恢复及回退完成对账；记录切换前后延迟、Google 请求数、积压和限额；P5 后续手机／Safari 验收接同一版本 |

目标代码组织：`cloudflare/` 放 Worker、DO、SQLite schema 与调度；`shared/` 放无 Google／Cloudflare 依赖的业务规则；`backend/` 保留 Apps Script 并逐步收敛为桥接；`contracts/` 放现行契约及迁移草案；`tests/` 按规则、DO、同步故障、真实 Google 和网页分层。C0 创建服务代码，核对已安装工具并锁定版本；依赖安装或 OAuth 完成不代表服务已经建立。

### C0 可执行工作清单

| 顺序 | 实现内容 | 完成证据 |
|---|---|---|
| C0.1 环境与骨架 | 固定 Wrangler／运行时版本；本地、staging、production 独立 Worker／DO 绑定、数据与 secret；建立 SQLite 类及应用 schema 版本，添加本地 secret／状态目录忽略规则 | 干运行可构建；空库初始化和旧 schema 升级可重复执行；staging 无生产 Google 文件绑定；默认开发命令不部署生产 |
| C0.2 契约与兼容 | 独立迁移契约草案定义动作、错误、任务状态、实例／代次、请求结果查询及权限；用虚构凭据建立旧摘要测试向量 | 与现行契约逐项对照；旧 Code／请求键／参数摘要一致；旧会话拒绝；私有结果不能匿名查询 |
| C0.3 原子提交 | 最小业务占位表、请求结果、审计、outbox 和任务表；业务及完整同步模型在 C1／C2 扩展 | 同号并发仅一次提交；不同参数拒绝；提交前故障全回滚，提交后丢响应仍能取回原结果；重启不丢状态 |
| C0.4 持久调度 | 到期排序、短事务领取、有界批次、租约／尝试编号、退避、alarm 恢复；先用假桥接 | 关闭所有页面后仍执行；模拟 Google 长时间失败及超过平台自动重试次数后继续续排；迟到回执、重启和重复唤醒不多执行 |
| C0.5 桥接小样 | 独立测试 Apps Script 的签名读回／回执查询；冻结协议字段，不迁移完整 Google 业务 | 真实 Worker ↔ Apps Script 往返，包含重定向；过期／篡改／错绑定／错代次被拒绝；不得返回或记录 secret |
| C0.6 阶段收尾 | 隔离 staging 冒烟、部署更新后读取持久数据、免费计划用量核对；记录命令及运维入口 | 新增 DO 测试、既有回归、前后端构建通过；形成 C0 验收记录并更新当前进度，才进入 C1 |

C0 的占位写入只验证持久化机制，不是训练报名 API，不向生产公开测试控制入口。C1 才验证真实最后名额竞争与全套规则。测试分为纯规则、真实本地 Workers／DO 运行时和远端隔离服务三层；本地用 Cloudflare 测试工具触发 alarm／驱逐对象，进程重启另验磁盘恢复，不能以 JS 内存模拟替代 SQLite 验收。C1 延续赛季时区、跨日及夏令时规则，不把当地日期运算改成固定毫秒天数。

## C4 写入交接与回退

1. 只读影子导入先盘点系统及所有赛季，导入稳定 ID、绑定、版本、成员覆盖值、当前状态、完整 revision／冻结快照、审计、已完成请求与归档索引，按记录数及规范化内容摘要对账。影子环境禁止对原 Sheet 导出和执行到期业务任务。
2. 在 Apps Script 的统一写锁内关闭旧业务写入、递增持久 `writer_epoch` 并暂停旧业务触发器；先完成既有可恢复请求，再做最终增量快照。切换窗口暂停人工编辑已绑定表格；Form 新回答仍留在原始响应区，由稳定来源键在新端补扫。
3. 最终核验 Cloudflare 数据、凭据和请求结果；初始化新的同步基线及任务。缺号、重复 ID、未恢复事务或未解决数据差异均阻止切换。
4. 启用新写入归属后才发布 Pages 的 Worker 地址和代次。旧 Apps Script URL 的队员／管理写入拒绝并提示刷新，旧只读入口返回迁移状态或读取新端公开结果；桥接和遗留脚本任务每次持锁核验归属，不能只关闭前端按钮。
5. 新后端确认成功的变化立即以 Cloudflare 为准。更新业务代码优先回退到兼容 SQLite schema 的上一个 Cloudflare 版本，不回到过时 Sheet。
6. 若必须退回 Apps Script，先停 Cloudflare 写入及任务，处理同步冲突、导出并核验最新状态／事件／请求结果，再在两端更新代次和旧端读取布局，最后恢复旧写入及页面。不能仅修改 API URL；完整对账失败则保持维护状态，不丢弃新端已确认记录。

所有外部副作用批次带归属代次及有效绑定，由桥接核验。服务间使用独立 secret、时间窗与签名／请求去重，不复用 Coach Code；Cloudflare 和 Google 各自保存服务端配置，日志不输出凭据。Cloudflare 生产资源由项目所有者长期控制的账号持有，可以是其个人邮箱账号；Google 资源继续由长期可访问的文件所有者持有。若未来发生交接，两个平台分别完成所有权、授权和恢复验证。

### 备份、保留与恢复

Google 运营副本会延迟，也不含全部请求和调度状态，不能作为 Cloudflare 的完整备份。C1 实现受保护的分批导出／校验工具；C4 切换前及 schema 变更前保存一致性快照和 manifest，包括 schema 版本、记录数量、摘要、业务／请求结果／事件／同步任务。快照包含私人数据，存放于仓库外的私有位置，密钥独立保管。

Cloudflare 的 SQLite PITR 提供近期时间点恢复，但不替代长期年度档案。C4 在远端隔离数据上演练恢复；先暂停两端写入及桥接、保存恢复前证据，再恢复并核对已在 Google 生效的批次。恢复后的旧 outbox 不得自动对 Google 重放，归属代次必须高于恢复前使用过的值。不能承诺恢复点之后的数据无条件存在；有缺口时列明受影响请求，不能直接宣布完整恢复。

首版不自动删除业务历史、请求去重结果或未确认任务；会话及已过期传输 nonce 可按有效期清理。后续清理请求结果必须先定义旧请求拒绝规则，避免把过期重试重新执行业务。原始 Form 回答由 Google 桥接按档案清单另行复制、核验，不因 DO 只保存必需字段而遗漏既定年度私有档案。

## 性能与维护验收

- 记录首次／再次读取、报名、换侧、排座保存和登录的样本数、p50／p95、错误及重试数；同一环境和数据规模比较 Apps Script 基线，不承诺未经测量的固定毫秒数。
- 比较同一业务操作序列下的 Google API 调用数与表格读写范围；确认网页读取不访问 Google、无变更不写、一次批次可合并多次业务变化。Form 及人工编辑检查仍会产生受控 Google 读取。
- 压测仅在隔离环境分档进行；测试一场训练的最后名额竞争、跨多个赛季的后台积压、有界分页、DO 重启和同步失败续跑。不得在生产制造长期大量请求。
- 免费额度、CPU、对象存储及 Google 配额建立简洁用量记录；到限额返回明确不可用并保留待同步任务。超过预算不自动升级付费，不承诺永久免费。C0 核对实际账号计划；重点测外层 Worker 的 Free HTTP CPU 限制、DO 请求／执行时长／SQLite 读写量和对象大小，不把静态站免费等同于整个后端无配额。
- 每个活跃赛季十分钟一轮即每天 144 轮检查，不是 144 次用户操作；每轮尽量合并 Form 补扫与业务范围读取，无变化不写业务行。同步等待也可能产生 DO 执行时长用量，须设置超时并关闭空转；上线容量结论依据实测数据，存储限额按部署时官方说明和账号能力核实。
- 观察期至少覆盖一个真实开放报名周期；首个自然结束测试赛季补验年度文件及荣誉墙。Safari／实体手机、管理员交接和 Google 文件权限继续保留为实证项，不用本地或模拟测试替代。

## 官方实现依据

- 团队对象与事务边界参考 [Durable Objects 设计规则](https://developers.cloudflare.com/durable-objects/best-practices/rules-of-durable-objects/) 和 [SQLite 存储 API](https://developers.cloudflare.com/durable-objects/api/sqlite-storage-api/)。DO 是持久状态协调服务；具体事务仍需正确封装，外部 I/O 不纳入原子业务提交。
- [Alarms](https://developers.cloudflare.com/durable-objects/api/alarms/) 每对象只有一个定时项，至少执行一次，自动失败重试有上限；本计划因此使用持久任务表、幂等及应用级续排。
- [Cloudflare 定价](https://developers.cloudflare.com/workers/platform/pricing/) 与 [DO 限制](https://developers.cloudflare.com/durable-objects/platform/limits/) 在部署和扩容时重新核对；容量规划按实际账号、计划和测试结果计算。
- [Google 可安装触发器](https://developers.google.com/apps-script/guides/triggers/installable) 不能作为所有修改方式的完整通知源；本计划保留周期差异检查和人工核查。
- [FormResponse](https://developers.google.com/apps-script/reference/forms/form-response) 提供回答 ID；[Form](https://developers.google.com/apps-script/reference/forms/form) 支持按时间读取回答，用于稳定来源去重及补扫。
- [Apps Script Web Apps](https://developers.google.com/apps-script/guides/web) 说明正文读取与执行身份；[Content Service](https://developers.google.com/apps-script/guides/content#redirects) 要求客户端处理 Google 重定向。
- [DO 类配置](https://developers.cloudflare.com/durable-objects/reference/durable-objects-migrations/) 与应用 SQL schema 升级分别管理；[测试 API](https://developers.cloudflare.com/workers/testing/vitest-integration/test-apis/) 用于 alarm／对象生命周期验收，PITR 须依 [SQLite API](https://developers.cloudflare.com/durable-objects/api/sqlite-storage-api/#pitr-point-in-time-recovery-api) 在远端验证。
- [Workers 限制](https://developers.cloudflare.com/workers/platform/limits/) 与 [DO 计费](https://developers.cloudflare.com/durable-objects/platform/pricing/) 分开核对，包含 alarm、SQLite 及等待期间的执行时长。
