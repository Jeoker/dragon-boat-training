# Dragon Boat Training 项目总览

> 未来 agent 的首要入口。最后核对：2026-09-19。
>
> 本页说明项目为什么存在、必须遵守的顶层原则、目标与子目标、当前架构边界，以及从哪里继续工作。会频繁变化的部署版本、实测结果和下一步只在[当前进度](CURRENT-STATUS.md)维护；完整业务规则只在[项目说明](README.md)维护。

## 1. 项目要解决什么

这是一个小型龙舟队的赛季制入队、训练报名和排座系统。它需要让普通队员顺畅完成日常报名，让 Coach、Steerer 和其他管理员使用同一套 Coach Mode 维护赛季、训练、队员、报名和座位，并把已经完成的训练长期保存为公开的队史荣誉墙。

项目成功的核心结果是：

1. 每个赛季有独立的入队 Form、季内成员资格、训练和报名，不直接继承旧季资格或报名状态；是否另建跨赛季队员历史关联，留待后续设计。
2. 管理员检查并发布本周计划后，队员才能看到和报名；没有发布的周表示赛季中的休息，不代表赛季结束。
3. 报名、候补、换侧、递补和排座在并发或重试时仍产生唯一、可解释的结果。
4. 管理员保存的草稿与队员看到的正式版本分离，训练结束 24 小时后冻结最终座位表。
5. 取消的训练视为不存在，不进入公开历史或年度归档；有效训练和整季结果长期保留。
6. 日常网页读取和保存最终不依赖 Google 的即时响应；Google Form／Sheets 继续承担入队入口、可人工核对的运营副本和私有归档。

## 2. 使用者与信任模型

| 使用者 | 能做什么 | 身份与权限边界 |
|---|---|---|
| 普通队员 | 看训练、报名、直接修改左右偏好、取消自己的名字、看正式座位表和荣誉墙 | 当前从本季名单选择姓名操作；团队接受代报名和误改风险，管理员可纠正 |
| Coach、Steerer、其他管理员 | 共用 Coach Mode，维护赛季、排期、成员、报名、角色、排座、发布、归档和同步冲突 | 每人使用个人 Coach Code；不区分 Coach 与管理员权限等级 |
| Google 文件维护者 | 直接管理 Form／Sheet、授权 Apps Script 或桥接服务 | Google 登录不代替 Coach Code；文件权限与网站管理权限分别维护 |
| Cloudflare 项目所有者 | 持有 Worker、Durable Object、部署及用量设置 | 可使用项目所有者长期控制的个人账号；密码、验证码、OAuth 凭据和 Account ID 不进入仓库 |

这是小团队的轻量权限模型。系统重点防止日常误操作、重复提交、旧页面覆盖新数据和无意滥用，不以抵御强对抗攻击为产品目标。凭据、文件 ID、会话令牌和服务端 secret 不进入仓库。

## 3. 顶层设计原则

### 3.1 赛季是最高业务边界

季内成员资格、Form／Sheet 绑定、训练、报名、排座、版本、日志和档案都必须带明确赛季归属。旧链接、空名单或无效赛季不得自动回退到另一赛季。赛季由管理员设置明确起止日期；到结束边界自动完成，而不是根据最后一场训练推断。未来即使建立长期队员身份或跨季历史关联，也不能破坏这层业务隔离。

### 3.2 发布决定公开状态

系统可以按模板生成一周草稿，但每周必须由管理员检查并确认。保存草稿不会公开；首次整周确认可立即或预约开放。周开放后新增的训练先保持私有，再由管理员单独发布。公开页面只展示已发布内容和正式座位版本。

### 3.3 服务器决定报名结果

浏览器不提前确认名额。后端在同一份当前状态上校验容量、截止、资格和版本，再决定确认或候补。候补递补按原报名时间先后，在适合空缺侧向的人中选择。直接修改偏好保留原报名时间，但放弃的旧侧名额可被别人递补；取消后重新报名使用新时间。

### 3.4 报名名额与具体船位分离

确认报名表示拥有训练名额，不等于已经分配船位。Coach／Steerer 由管理员设置，同一人可以兼任两个带队角色，但不能同时占用划桨座位。排座草稿仅管理员可见，发布后的正式版本才对队员公开。

### 3.5 所有写入可重试、可恢复、可审计

业务写入使用请求编号、版本检查、确定结果和操作记录。结果未知时用同一编号核实或重试，不生成第二个业务动作。并发冲突必须拒绝静默覆盖；恢复过程不能重新生成排队时间、递补或发布 revision。

### 3.6 在线业务与 Google 副本解耦

迁移完成后，Cloudflare 持久化成功就是网页业务成功；普通读取、报名、换侧和排座不等待 Google。Google 的更新通过持久 outbox 和可重试批次完成，正常目标是在有变化时约十分钟同步，而不是把浏览器内存或 GitHub Pages 当作共享缓存。

### 3.7 支持偶尔直接修改 Sheet，但不盲目覆盖

系统比较上次同步基线、Cloudflare 当前值和 Sheet 当前值。合法、独立的业务字段变化可以导入；相关变化发生冲突时由 Coach Mode 预览和确认。删除行、修改稳定 ID、容量、系统状态、版本、日志或冻结记录不能直接成为业务事实。

### 3.8 历史以冻结快照为准

训练结束后 24 小时内，管理员仍可修正并发布最终座位表；边界到达后立即停止直接编辑，以最后正式版本冻结。归档任务即使稍后运行，也不能延长更正时间。公开荣誉墙只包含允许公开的快照字段，完整档案保持私有。

## 4. 目标与子目标

| 能力 | 子目标 | 主要工作归属 |
|---|---|---|
| 赛季与入队 | 建季、日期和时区、独立 Form／Sheet 绑定、自动激活、名单导入、默认赛季、结束与归档 | BE-03、ADM-02 |
| 每周排期 | 默认模板、周草稿、检查确认、预约开放、开放后单场增补、改期和取消 | BE-04、FE-02、ADM-03 |
| 报名与候补 | 从本季名单选名、Left／Ambient／Right、容量、顺序、换侧、取消、管理代办和递补 | BE-05、FE-03、ADM-04 |
| 排座与角色 | 10 排左右船位、待排座池、Coach／Steerer、草稿、发布、系统修订和 24 小时最终更正 | BE-06、FE-04、ADM-05 |
| 历史与档案 | 单场冻结、年度私有文件、整季档案、公开荣誉墙、分页和更正说明 | BE-07、FE-05、ADM-06 |
| 可靠性与运维 | 会话、权限、幂等、冲突、恢复、日志、分页、负载边界、部署和管理员交接 | BE-01／02／08、FE-01／06、ADM-01／06 |
| Cloudflare 在线数据层 | Worker、团队 Durable Object、SQLite、统一规则、持久任务和 outbox | BE-09、FE-07 |
| Google 双向同步 | Form 导入、基线比较、最小补丁、回执恢复、人工修改检测、冲突预览 | BE-10、ADM-07 |
| 受控迁移 | 影子导入、完整对账、写入归属交接、旧端点防误写、回退和用量观察 | BE-11、FE-07、ADM-07 |

三个 Epic 的固定边界如下：

- **BE** 负责规则、数据、权限、接口、Google 桥接、任务、迁移和恢复。
- **FE** 负责公开页面、共享展示组件、唯一客户端、网站构建和 Pages 发布。
- **ADM** 负责同一网站中的 Coach Mode 与管理交互，不创建第二套权限、传输或业务算法。

详细工作包、依赖和验收门槛见[Epic 总览](epics/README.md)。

## 5. 当前架构与目标架构

不要把已经确认的目标架构写成当前生产事实。

| 层次 | 当前生产基线 | C4 切换后的目标 |
|---|---|---|
| 网页 | GitHub Pages 上的 Astro 静态站 | 继续使用 GitHub Pages |
| 在线 API 与业务规则 | Google Apps Script | Cloudflare Worker + 团队 Durable Object |
| 在线业务主数据 | Google Sheets | Durable Objects SQLite |
| 入队来源 | Google Form 回答导入 Sheet | Google Form 经 Apps Script 桥接导入 Cloudflare |
| Google Sheets | 当前业务权威、运营数据和归档 | 可核对的运营副本及私有归档，允许受控人工修改 |
| 网页保存是否等待 Google | 是，受 Apps Script／Sheets 延迟影响 | 否，Cloudflare 提交成功即可确认；Google 异步同步 |

目标运行链路：

```text
队员／Coach 浏览器
        │
        ▼
GitHub Pages 静态网页
        │ HTTPS API
        ▼
Cloudflare Worker → Team Durable Object → SQLite
                              │
                              └→ 持久同步任务／outbox
                                         │ 签名桥接
                                         ▼
                             Apps Script → Form／Sheets／年度档案
```

GitHub Pages 本身不能保存所有用户共享的可变状态，也不能承担定时可靠写回；它只是网页托管层。共享缓存和在线数据库属于 Cloudflare 数据服务。

## 6. 交付路线与当前状态

### 已建立的现行产品基线

P0 至 P4 已完成相应阶段验收：连接与契约、建季排期、报名维护、排座最终更正、历史归档。P5 的第一批性能和稳定性改进已部署，包括连续排座合并保存、有界后台任务、日志／历史分页及只读历史缓存。

截至本页最后核对时，线上仍是 Apps Script／Sheets 架构。长期 Google 负载、非空分页、Safari、实体手机、管理员交接和首个自然结束赛季的年度文件仍保留实证边界。精确版本、测试数量、线上证据和测试数据状态必须读取[当前进度](CURRENT-STATUS.md)，不要从本页推断。

### 已确认、分阶段实施的迁移路线

| 阶段 | 目标 |
|---|---|
| C0 | 建立 Worker、Durable Object、SQLite、事务、请求去重、alarm 和 API 代次的最小闭环 |
| C1 | 依次迁移赛季／成员、排期、报名候补、排座和冻结历史规则，接入 outbox |
| C2 | 完成 Google Form／Sheets 桥接、三方比较、双向同步、故障恢复和年度归档导出 |
| C3 | 在 Coach Mode 增加同步概览、暂停导出、冲突预览与解决，并完成双端全流程 |
| C4 | 影子迁移和对账后切换唯一写入归属，阻止旧端点误写，演练保留新数据的回退 |

**C0 已验收，下一开发阶段是 C1 业务迁移；没有部署 Cloudflare 生产 Worker，也没有改变生产写入归属。** 精确完成度必须读取[当前进度](CURRENT-STATUS.md)。只有 C4 门槛全部通过后，Cloudflare 才能被描述为生产业务权威。完整设计和同步协议见[Cloudflare 迁移计划](cloudflare-migration-plan.md)。

## 7. 明确不在当前范围内的内容

- Coach 与管理员的分级权限体系。
- 多船排座、签到统计或自动消息通知。
- 允许管理员在已运营赛季任意替换绑定文件，或由网站自动改变 Form 的响应目标。
- 把任意 Sheet 行删除、ID 修改或系统字段编辑自动解释成有效业务操作。
- 承诺 Cloudflare 或 Google 永久在线、零延迟或永远免费。

当前仍有两个开放设计项：

- **D5：正式队名字样、高清 Logo 和页面语言。** 实现可继续使用现有主题色和占位 Logo，收到正式素材后替换。
- **D6：队员长期身份与跨赛季历史。** 当前报名流程和各季业务记录继续独立，但本项目不限制未来增加队员账号、身份核验、同名处理、跨季历史关联或资料复用；具体目标、隐私边界和迁移方式尚未决定。

## 8. 文档权威顺序

| 需要了解的内容 | 唯一主要维护位置 |
|---|---|
| 项目入口、目标、原则、架构边界和能力地图 | 本页 |
| 完整产品规则和待定事项 | [README.md](README.md) |
| 已实现、已部署、已验证、未验证及下一步 | [CURRENT-STATUS.md](CURRENT-STATUS.md) |
| C0–C4 架构、同步和切换／回退门槛 | [cloudflare-migration-plan.md](cloudflare-migration-plan.md) |
| Epic 边界、工作包、依赖和阶段验收 | [epics/README.md](epics/README.md) |
| 页面与交互细节 | [frontend-spec.md](frontend-spec.md) |
| 现行 Apps Script 数据模型与恢复协议 | [google-sheets-backend-spec.md](google-sheets-backend-spec.md) |
| 后端构建、部署和真实测试限制 | [backend/README.md](backend/README.md) |
| 某阶段的历史验收证据 | `tests/*-ACCEPTANCE.md` |

发生差异时，先判断差异属于产品规则、当前状态、目标架构还是历史证据，再修改对应的唯一维护位置。历史验收报告只证明当时版本，不覆盖后续规则或当前部署。

## 9. 未来 agent 的开始清单

1. 先读本页，再读[当前进度](CURRENT-STATUS.md)和当前任务对应的规格或 Epic；不要只依据聊天记录开始实现。
2. 确认工作目录是独立仓库 `D:\agents\dev-master\dragon-boat-training`，分支和未提交改动均已检查；`D:\agents\dev-master` 只是多项目容器。
3. 区分“设计已确认”“本地已实现”“真实 Google／Cloudflare 已验收”“生产已切换”四种状态；缺少证据时不得向后提升。
4. 修改业务规则前先更新 `README.md`，修改阶段或实现边界时更新对应 Epic／规格，完成实际验证后才更新 `CURRENT-STATUS.md`。
5. 保留稳定 ID、报名时间、版本、请求结果、正式 revision、冻结快照、审计和恢复语义；不要在客户端或新后端复制第二套报名／递补算法。
6. 继续写入真实测试环境前重读服务器状态和验收脚本限制。不要假设旧的成员人数、草稿版本、会话或测试清理状态仍然成立。
7. 本地基线命令为 `npm test`、`npm run build`、`npm run build:backend`；文档变更至少检查链接和 `git diff --check`。
8. 不提交 Coach Code、Cloudflare／Google 密钥、OAuth 结果、私有文件 ID、会话令牌或真实成员隐私数据。
